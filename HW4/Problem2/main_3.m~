clear all;
close all;
clc;

m1 = .05;
m2 = .05;

% intial and final positions in meters
x2_i(:,1) = IK(.03,.04)
x2_f(:) = IK(-.03,.45)

x_path = planTraj(x2_i(1),x2_f(1), 3);
y_path = planTraj(x2_i(2),x2_f(2),3);